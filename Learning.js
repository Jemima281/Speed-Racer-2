/*Goal 1:

1. Update game state to 1. When both the players joined.both


read the database(about gamestate and playercount)
update gamestate
count the players
start the game
 
Database:
REF()
once() - only one time
Read: on //listener
.on("value", function(dataAvesta){
    dataAvesta.val()
})
Write
set() = get rid of everything then rewrite it just to add 
update() = add one piece of info to the database
*/